# MANTA: Enhancing Mamba for Few-Shot Action Recognition
# RTX 5090 (Blackwell - sm_120) Compatible Requirements

# ======================================================================
# BƯỚC 1: Cài đặt PyTorch Nightly (cu128) để hỗ trợ RTX 5090
# Chạy lệnh này ĐẦU TIÊN (nếu chưa cài):
# pip install --pre torch torchvision torchaudio --index-url https://download.pytorch.org/whl/nightly/cu128
# ======================================================================

# ======================================================================
# BƯỚC 2: Cài đặt các thư viện tiêu chuẩn
# Chạy lệnh: pip install -r requirements.txt
# ======================================================================

# Vision
timm==0.9.16
einops==0.7.0

# Video processing
opencv-python==4.9.0.80
decord==0.6.0
av==12.0.0

# Scientific
numpy==1.26.4
scipy==1.13.0
scikit-learn==1.4.2

# Training utilities
tensorboard==2.16.2
tqdm==4.66.2
pyyaml==6.0.1
omegaconf==2.3.0

# Optional: experiment tracking
wandb==0.17.0
matplotlib
seaborn

# ======================================================================
# BƯỚC 3: Compile thư viện CUDA (Mamba-SSM và Causal-Conv1d)
# Chạy lệnh này SAU CÙNG. Bắt buộc phải có --no-build-isolation để 
# ép thư viện compile bằng đúng bản PyTorch 2.11 vừa cài ở Bước 1.
#
# Lệnh:
# pip install causal-conv1d>=1.4.0 mamba-ssm>=2.2.2 --no-build-isolation
# ======================================================================
